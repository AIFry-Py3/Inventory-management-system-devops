pipeline {
    agent any

    stages {
        stage('Run Streamlit') {
            steps {
                sh '''
                pip install streamlit
                streamlit run app.py --server.port 8501 --server.address 0.0.0.0
                '''
            }
        }
    }
}
